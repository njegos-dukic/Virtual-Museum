.header-container {
    display: flex;
	flex-direction: row;
	height: calc(35px + 12vh);
    min-height: 75px;
	width: 100%;
	background-color: #c50e29;
    border-radius: 12px;
}

.header-login-container {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 25%;
}

.header-login-image {
	height: calc(30px + 2vw);
}

.header-login-label {
    color: #ffffff;
    white-space: nowrap;
    font-size: min(calc(8px + 2vw), 25px);
    font-family: 'Bebas Neue', cursive;
}

.header-logo-container {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 15%;
}

.header-logo {
	height: calc(30px + 2vw);
}

.header-title {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60%;
	color: #ffffff;
    white-space: nowrap;
    font-size: calc(10px + 2vw);
    font-family: 'Bebas Neue', cursive;
}

